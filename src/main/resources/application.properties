spring.profiles.active=${APP_PROFILE:test}
spring.jpa.open-in-view=false

cors.origins=${CORS_ORIGINS:http://localhost:4200,http://localhost:8080}

security.oauth2.client.client-id=${CLIENT_ID:ecommerceapp}
security.oauth2.client.client-secret=${CLIENT_SECRET:ecommerceapp123}

jwt.secret=${JWT_SECRET:MY-JWT-SECRET}
jwt.duration=${JWT_DURATION:86400}